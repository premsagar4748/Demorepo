import { ParenBracketClass } from './paren-bracket-class';

describe('ParenBracketClass', () => {
  it('should create an instance', () => {
    const directive = new ParenBracketClass();
    expect(directive).toBeTruthy();
  });
});
